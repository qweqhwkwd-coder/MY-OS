# MY-OS — Project Memory (CLAUDE.md)

> Личная «ОС для жизни». Telegram-бот + FastAPI + Supabase + Mini App (React).
> Деплой: бэкенд на Render (free), Mini App на GitHub Pages.
> **Текущее состояние:** MVP-ядро + 12 из 17 модулей продукта — в боте. Mini App
> покрывает 7 экранов ядра + профиль с RPG (ранг/HP/стрик). AI-слой не подключён.
> Подробно и всегда актуально — `docs/claude/current-state.md`.

Этот файл — точка входа. Детали по доменам — в `docs/claude/` (читай только нужный файл, не всё подряд).

---

## Что это

MY-OS — персональная система трекинга жизни (тренировки, питание, задачи, привычки, финансы, рефлексия) с RPG-слоем: каждое действие даёт XP в одну из 8 характеристик. Цель v1 — один пользователь (автор). Архитектура с RLS заложена под мульти-юзер на будущее.

Полное продуктовое видение (17 модулей) — `MY-OS_product_document.md`.
Рабочий план постройки — `ROADMAP_v2.md`.
Технический срез состояния — `HANDOFF.md`.

---

## Карта документации

| Файл | Когда читать |
|---|---|
| `docs/claude/architecture.md` | Стек, процесс, как устроен бэкенд, БД, деплой |
| `docs/claude/current-state.md` | Что реально работает в коде СЕЙЧАС (источник правды по статусу) |
| `docs/claude/modules.md` | Все 17 модулей: что делает каждый, статус, RPG-влияние |
| `docs/claude/rpg-system.md` | 8 характеристик, начисление XP, уровни, ранги |
| `docs/claude/database.md` | Схема таблиц, RLS, миграции |
| `docs/claude/security.md` | Безопасность: секреты, изоляция данных, мультиюзер, продажа |
| `docs/claude/ai-layer.md` | AI-провайдеры, голос, vision, AI-тренер (пока не реализовано) |
| `docs/claude/roadmap.md` | Фазы, что отложено, узлы для решения перед фазами |
| `docs/claude/conventions.md` | Стиль кода, дизайн-система, язык, известные проблемы |

---

## Быстрый старт (команды)

```bash
# Локально (нужен туннель — бот работает только через вебхук, см. architecture.md)
cp .env.example .env          # заполнить значения
pip install -r backend/requirements.txt
cd backend && uvicorn main:app --reload

# Проверка синтаксиса (автотестов нет)
python -m py_compile backend/*.py

# Проверка живости прода
curl https://<service>.onrender.com/health   # → {"status":"ok"}
```

Миграции применяются ВРУЧНУ через Supabase SQL Editor, по одному файлу за раз. См. `docs/claude/database.md`.

---

## Критичные правила (не нарушать)

1. **`@dp.message()` catch-all (`on_any`) регистрируется ПОСЛЕДНИМ.** Иначе перехватит все команды.
2. **Миграции — по одной, отдельным запуском.** Повторный `create table` падает с `relation already exists`.
3. **Бэкенд ходит под `service_role` — RLS сейчас «спит».** Не полагаться на RLS как на защиту в текущем коде.
4. **Принцип разработки: одна маленькая рабочая функция за раз.** Не строить несколько модулей параллельно. См. `roadmap.md`.
5. **Никаких секретов в репозитории.** `.env` в `.gitignore`. Значения только в Render → Environment. `service_role` никогда не уходит на клиент.
6. **Данные юзера чувствительны** (здоровье, финансы, личное). При любой работе с доступом к данным сверяться с `docs/claude/security.md`. Перед вторым пользователем — обязательна реальная изоляция (RLS + мост initData→JWT).
7. **Коммить после каждого завершённого изменения.** После любой рабочей правки — `git add` + `git commit` с понятным сообщением. Не накапливать несвязанные изменения в одном коммите.
8. **Использовать плагин `frontend-design:frontend-design` при любой работе с UI/дизайном.** Перед созданием или изменением любого фронтенд-компонента, страницы, стилей — invokeить этот skill. Без него нельзя трогать дизайн.
9. **Использовать Superpowers при любой работе с бэкендом.** Перед добавлением/изменением backend-кода — invokeить релевантный skill: `superpowers:brainstorming` (новая фича), `superpowers:systematic-debugging` (баг), `superpowers:test-driven-development` (новый хендлер/эндпоинт), `superpowers:requesting-code-review` (после завершения).
10. **После каждого завершённого задания/этапа/чекпоинта — анализ изменений.** Запустить `superpowers:requesting-code-review`, зафиксировать что сделано, что изменилось, какие риски появились. Только после анализа — переходить к следующему шагу.
11. **После каждого этапа, меняющего реальный статус фичи/модуля — сразу обновлять документацию.** Если что-то стало работать (или перестало), отразить это в `docs/claude/current-state.md` (и в затронутых файлах `docs/claude/`, `HANDOFF.md`, `README.md`) в том же шаге, не «когда-нибудь потом». 21.06.2026 был сделан полный аудит: документация отстала от кода на 2–3 фазы (Mini App считался несозданным, хотя давно существовал, и т.п.) — это правило существует, чтобы не повторилось.
