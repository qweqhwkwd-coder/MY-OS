# Текущее состояние (источник правды по статусу)

> Назад: [CLAUDE.md](../../CLAUDE.md)
> Это самый важный файл для понимания «что реально есть». Легенда: ✅ работает · 🟡 частично · ❌ нет кода.
> Этот файл долго не обновлялся вместе с кодом (см. полный аудит в истории чата/коммитов) — ниже сверено напрямую с `backend/*.py`, `miniapp/src/`, `db/migrations/`.

## Что работает прямо сейчас

**Backend — два клиента: Telegram-бот (aiogram, `main.py`) и REST API для Mini App (`api_routes.py`).**

| Модуль | Статус | Детали |
|---|---|---|
| Регистрация `/start` | ✅ | `ensure_user` создаёт юзера + строку статов |
| Дашборд `/today` + `/api/today` | ✅ | вода/ритуалы/задачи/ккал за день |
| Вода `/water` | ✅ | inline +250/+500/+1000, прогресс-бар, +2 XP в Здоровье при достижении цели (один раз в день) |
| Ритуалы `/addritual` `/rituals` `/delritual` | ✅ | список, отметка ✅/⬜, стрик x/7, +2 XP в Дисциплину |
| Задачи `/addtask` `/tasks` `/deltask` | ✅ | добавление, список, отметка выполнено, удаление по названию, +3 XP в Дисциплину |
| Питание `/addfood` `/food` | ✅ | название + граммы (опц.) + ккал, лог за день, +2 XP в Питание |
| Дневник `/journal` | ✅ | текст + настроение 1–5, архив, поиск по дате |
| Цели `/addgoal` `/goals` `/goalsdone` | ✅ | название, опц. дедлайн, выполнение → +10 XP Дисциплина +5 XP Рефлексия |
| Идеи `/idea` | ✅ | быстрая запись, список |
| Встречи `/addmeeting` `/meetings` | ✅ | название, дата, опц. время, список ближайших |
| Сон `/sleep` | ✅ | время засыпания/пробуждения → длительность, +3 XP Здоровье при 7–9ч |
| Тренировки `/workout` `/workouts` | ✅ | активность + опц. минуты, авто-определение типа (кардио/силовое/гибкость) по ключевым словам, XP в соответствующий стат |
| Финансы `/spend` `/finance` | ✅ | сумма + категория, сводка за 7 дней по категориям |
| Замеры тела `/weight` | ✅ | вес, история последних замеров |
| Колесо баланса `/balance` | ✅ | 8 сфер по 1–10 через инлайн-кнопки |
| Inbox `/inbox` | ✅ | нераспознанные сообщения падают сюда автоматически, отметка обработанным |
| Дайджест `/digest` | ✅ | сводка за 7 дней по всем модулям (без AI) |
| RPG-движок | ✅ | 8 статов, XP, level, **ранги** (`get_rank`, 10 тиров), **HP** (`calculate_hp`, по воде/ритуалам/сну/еде за 3 дня), **стрик** (`get_streak`) |
| `/stats` | ✅ | все 8 статов с уровнями |
| Reply Keyboard | ✅ | 6 кнопок-шорткатов |
| **Mini App (React)** | ✅ | SwipeRow во всех блоках, архив задач, КБЖУ-калькулятор, дата-фильтр дневника, прогресс-бар ккал |
| **XP/HP feedback layer** | ✅ | тосты `xp_granted` за действия, майлстоуны через диффинг профиля (level/rank/стрик 7-30-100/стат-lvl кратно 5/totals 10-50-100), график XP за 30 дней (recharts, lazy-чанк), `GET /api/xp-history` |
| Уведомления / планировщик | ❌ | отложено (free-хостинг, см. roadmap.md) |
| AI-слой | ❌ | не подключён |
| Мультипользователь | ❌ | моста initData→JWT нет, бэкенд под `service_role` |
| Умные уведомления (модуль 16) | ❌ | требует планировщика |
| Полный 5-шаговый онбординг (физ-профиль, КБЖУ) | 🟡 | `WelcomeScreen` + вкладка «Тіло» в ProfileModal с КБЖУ-калькулятором (Mifflin, обе формулы — стать выбирается в профиле, миграции 016/017) |
| Настройки (модуль 17) | 🟡 | только переключение темы (light/dark/auto) в `ProfileModal`; язык/часовой пояс/экспорт/уведомления — нет |
| AI-тренер, шаблоны тренировок | ❌ | только ручной лог тренировки |
| Чтение/учёба (источник XP в Интеллект) | ❌ | стат `intellect` существует, но ничего его не начисляет |

## Mini App — реально существует и развивается

`miniapp/` — React 19 + Vite + Tailwind v4, задеплоен статикой на GitHub Pages
(`https://qweqhwkwd-coder.github.io/MY-OS/`), открывается кнопкой в боте через `WebAppInfo`.

Экраны/компоненты (`miniapp/src/`):
- `App.tsx` — роутинг между вкладками, темизация (CSS-переменные `--bg`/`--ink`/...), опрос Telegram WebApp SDK на готовность initData.
- `components/WelcomeScreen.tsx` — онбординг-экран с цитатой дня, показывается при каждом открытии (не раз в день).
- `components/SwipeRow.tsx` — универсальный свайп-компонент (свайп влево → кнопки действий), заменяет LongPressButton во всех экранах.
- `components/SysBar.tsx`, `components/NavGrid.tsx` — верхняя системная панель + сетка навигации по модулям.
- `components/ProfileModal.tsx` — профиль (ранг, percentile, HP, стрик, XP за сегодня, 8 статов) + вкладка настроек + вкладка «Тіло» с КБЖУ-калькулятором (формула Миффлина-Сент-Жора).
- `components/ErrorBoundary.tsx` — видимый экран краша вместо белого экрана.
- `components/Toast.tsx` — ToastProvider/useToast (первый React Context в проекте), верх экрана, `aria-live`, sm 2.5с / lg 4с.
- `components/XpChart.tsx` — LineChart XP за 30 дней (recharts), lazy-чанк чтобы не раздувать основной бандл (252KB против 599KB).
- `pages/Today.tsx` — дашборд с прогресс-баром калорий vs цель КБЖУ.
- `pages/Tasks.tsx` — SwipeRow + вкладки Активні/Архів (ленивая загрузка) + BottomSheet-редактирование + line-clamp-2.
- `pages/Rituals.tsx` — SwipeRow вместо LongPress, редактирование через BottomSheet.
- `pages/Notes.tsx` — SwipeRow (Опції/Видалити), конвертация через BottomSheet.
- `pages/Diary.tsx` — SwipeRow + фильтр по дате + редактирование/удаление через BottomSheet.
- `pages/Food.tsx` — SwipeRow удаление + прогресс-бар ккал/цель.
- `pages/Water.tsx` — без изменений.
- `pages/Sleep.tsx` — сегодняшний сон + форма ЧЧ:ММ→ЧЧ:ММ + история 7 дней (`GET/POST /api/sleep`, upsert по дате).
- `pages/Finance.tsx` — траты за 7 дней: сводка по категориям с барами, форма добавления, список операций (`GET/POST /api/finance`).
- `pages/Workouts.tsx` — форма (активность + минуты) с автоопределением типа, последние 10 (`GET/POST /api/workouts`).
- `api.ts` — тонкий fetch-клиент к `/api/*`, шлёт `X-Telegram-Init-Data` заголовком. Новые методы: `archivedTasks`, `diaryForDate`, `updateDiaryEntry`, `deleteDiaryEntry`, `deleteFoodEntry`, `getBodyProfile`, `updateBodyProfile`, `xpHistory`. Мутации, начисляющие XP, возвращают `xp_granted`.
- `utils.ts` — `STAT_LABEL`/`STAT_NAME`, `xpToastText`, чистая `profileMilestones(prev, next)` (диффинг снимков профиля; в `App.tsx` защита от гонки конкурентных фетчей через seq-guard).

Стек по факту (не путать с целевым из `conventions.md`): обычный `useState`/`useEffect`,
recharts внедрён (только для XP-графика, lazy). Без Zustand/TanStack Query/shadcn/ui — это всё ещё в планах.
Пакеты `@telegram-apps/telegram-ui` и `@telegram-apps/sdk-react` удалены (не использовались, peer-конфликт с React 19).

## Язык интерфейса — украинский (подтверждено автором)

И бот (`main.py`), и Mini App — тексты на **украинском** («Напиши назву»,
«Невірний формат», «Світла»/«Темна» в теме). Это целевой язык, не баг и не
случайный разъезд — старая документация утверждала «жёстко зашитый русский»,
это было неверно. UA — основной язык интерфейса; RU/EN остаются в планах на
будущий i18n как дополнительные.

## Таблицы, существующие в БД

Из примененных миграций `001`–`015` (см. `database.md`):

`users` (+ колонки `weight_kg`, `height_cm`, `age`, `activity_level` — миграция `015_body_profile.sql`, применить вручную в Supabase SQL Editor перед деплоем!), `user_stats`, `xp_events`, `tasks`, `rituals`, `ritual_logs`, `water_logs`,
`food_logs`, `sleep_logs`, `transactions`, `diary_entries`, `body_measurements`,
`ideas`, `goals`, `workouts`, `meetings`, `inbox_items`, `life_balance`.

**⚠️ Миграции 015, 016, 017 не применены в Supabase** — запустить по одной через SQL Editor:
`015_body_profile.sql` → `016_sex_column.sql` → `017_sex_check.sql`.
**Пока 015+016 не применены, бэкенд 500-ит на `/api/today`, `/api/profile`, `/api/users/body`** (код уже селектит колонки `weight_kg…sex`, Render деплоит раньше ручных миграций).

Плюс Postgres-функция `increment_user_stat` (миграция `014`) — атомарное
начисление XP, см. `database.md`.

## Известные баги / долги (сверено с кодом)

1. ~~Фарм XP на ритуалах~~ — **пофикшено.** `toggle_ritual` при снятии отметки
   делает `is_done = False`, лог не удаляется; повторная отметка XP не даёт.
2. ~~Нет отмены воды~~ — **пофикшено 02.07.2026.** `POST /api/water/undo` + кнопка
   «Відмінити останнє» (Вода + quick-add). XP за воду и сон теперь once-per-day
   через `xp_granted_today()` (защита от фарма через undo/перезапись).
3. **Синхронный `supabase-py` в async-обработчиках бота** — блокирует event loop
   (см. `architecture.md`). Всё ещё открыто — ниже исправлены только последовательные
   запросы внутри одного REST-запроса, не сама синхронность клиента в aiogram-хендлерах.
4. **Нет автотестов.** Только `python -m py_compile` (+ ad-hoc ручная проверка при рефакторинге).
5. **Нет `stats_history`** → нет графиков динамики статов.
6. Существовал черновик схемы на 27 таблиц в заброшенном каталоге — игнорировать,
   боевая БД сверяется с реальными файлами в `db/migrations/`.
7. ~~`ensure_user` дублировался ~38 раз по хендлерам бота~~ — **пофикшено.**
   `EnsureUserMiddleware` (`main.py`) резолвит юзера один раз на update через
   `outer_middleware`, хендлеры получают `user: dict` параметром.
8. ~~`/api/today`, `/api/profile`, `get_week_digest` делали 4–8 последовательных
   запросов к Supabase~~ — **пофикшено.** `db.parallel()` (ThreadPoolExecutor)
   гонит независимые запросы параллельно вместо по одному.

## Что следующее по плану

MVP-ядро и большинство модулей продукта (12 из 17, см. `modules.md`) уже в коде.
Mini App покрывает 10 экранов (Сьогодні/Вода/Ритуали/Завдання/Їжа/Нотатки/Щоденник/Сон/Фінанси/Тренування); Цілі/Ідеї/Зустрічі/Дайджест — только через бота (🔒 в NavGrid).

**Ближайший обязательный шаг:** Применить миграцию `db/migrations/015_body_profile.sql` в Supabase SQL Editor.

Следующие возможные направления:
- ~~XP/HP feedback layer~~ — **сделано 02.07.2026** (спек `docs/superpowers/specs/2026-06-22-gamification-feedback-design.md`, коммиты 23c6627..ecc7d64 + фиксы ревью)
- ~~Экраны Сон/Фінанси/Тренування в Mini App~~ — **сделано 02.07.2026** (d4060cf + 58d47d3)
- ~~Стать в КБЖУ + отмена воды~~ — **сделано 02.07.2026** (f8f95aa + 18cf975 + фиксы ревью)
- AI-слой (Фаза 4, см. `roadmap.md`)
- Добавить стать в КБЖУ-профіль
